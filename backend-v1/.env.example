# Copy this file to .env and adjust as needed for local development.

SPRING_PROFILES_ACTIVE=dev

# Database (used by docker-compose and application-*.yml)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=crm_db
DB_USERNAME=crm_user
DB_PASSWORD=crm_password
